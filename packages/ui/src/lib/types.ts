import { DateRange as ReactDayPickerDateRange } from 'react-day-picker';

// Re-export DateRange type from react-day-picker
export type DateRange = ReactDayPickerDateRange;

// Other types will be imported from their respective component files
// to avoid duplication and type conflicts